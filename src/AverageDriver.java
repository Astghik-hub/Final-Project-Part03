/**
 * Class that contains the main method for Average
 */
public class AverageDriver {
    /**
     * The main method, declares and instantiate an Average object and prints it information
     */
    public static void main(String[] args) {
        Average average = new Average();
        System.out.println(average.toString());
    }
}
